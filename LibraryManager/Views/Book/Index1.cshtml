@model System.Data.DataTable
@{
    ViewBag.Title = "Index1";
}
<table class="table">
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Genre Name</th>
        <th></th>
    </tr>
    @for (int i = 0; i < Model.Rows.Count; i++)
    {
<tr>
    <td> @Model.Rows[i][0]</td>
    <td>  @Model.Rows[i][1]</td>
    <td>  @Model.Rows[i][2]</td>
    <td>
        <a href="@Url.Action("Edit1","Book",new { @id = @Model.Rows[i][0]})">Edit</a>
        <a href="@Url.Action("Delete","Book",new { @id = @Model.Rows[i][0]})">Delete</a>
    </td>

</tr>
    }

</table>

<div>@Html.ActionLink("Back", "Index", "Library")</div>
<div>@Html.ActionLink("Create Book", "Create1", "Book")</div>


